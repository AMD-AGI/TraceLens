diff --git a/python/sglang/srt/utils/profile_utils.py b/python/sglang/srt/utils/profile_utils.py
index 7b6bcac31c..018ed0206f 100644
--- a/python/sglang/srt/utils/profile_utils.py
+++ b/python/sglang/srt/utils/profile_utils.py
@@ -8,6 +8,7 @@ from typing import Callable, Dict, List, Optional
 
 import torch
 
+import sglang.srt.utils.kernel_shape_profiler as kernel_shape_profiler
 from sglang.srt.distributed.parallel_state_wrapper import ParallelState
 from sglang.srt.managers.io_struct import ProfileReqOutput
 from sglang.srt.model_executor.forward_batch_info import ForwardMode
@@ -62,6 +63,7 @@ class ProfileManager:
         merge_profiles: bool,
         profile_prefix: str,
         profile_stages: Optional[List[str]] = None,
+        shape_discovery: bool = False,
     ):
         # not supported yet
         assert start_step is None
@@ -82,6 +84,7 @@ class ProfileManager:
             output_dir=output_dir,
             output_prefix=profile_prefix,
             profile_id=profile_id,
+            shape_discovery=shape_discovery,
         )
 
         self.stage_based_trigger.configure(
@@ -196,6 +199,9 @@ class _StageBasedTrigger:
 class _ProfilerBase(ABC):
     @staticmethod
     def create(activities, with_stack, record_shapes, **kwargs):
+        # shape_discovery only applies to the torch profiler; consume it here so
+        # the other inner profilers (memory / cudart / rpd) don't receive it.
+        shape_discovery = kwargs.pop("shape_discovery", False)
         inners = []
         if ("CPU" in activities) or ("GPU" in activities):
             inners.append(
@@ -204,6 +210,7 @@ class _ProfilerBase(ABC):
                     activities=activities,
                     with_stack=with_stack,
                     record_shapes=record_shapes,
+                    shape_discovery=shape_discovery,
                 )
             )
         if "MEM" in activities:
@@ -256,11 +263,19 @@ class _ProfilerConcreteBase(_ProfilerBase):
 
 
 class _ProfilerTorch(_ProfilerConcreteBase):
-    def __init__(self, with_stack: bool, record_shapes: bool, activities, **kwargs):
+    def __init__(
+        self,
+        with_stack: bool,
+        record_shapes: bool,
+        activities,
+        shape_discovery: bool = False,
+        **kwargs,
+    ):
         super().__init__(**kwargs)
         self.with_stack = with_stack
         self.record_shapes = record_shapes
         self.activities = activities
+        self.shape_discovery = shape_discovery
 
     def start(self):
         activity_map = {
@@ -283,12 +298,20 @@ class _ProfilerTorch(_ProfilerConcreteBase):
                 else torch_npu.profiler.tensorboard_trace_handler(self.output_dir)
             ),
         )
+
+        if self.record_shapes and self.shape_discovery:
+            kernel_shape_profiler.enable()
+
         self.torch_profiler.start()
 
     def stop(self):
         Path(self.output_dir).mkdir(parents=True, exist_ok=True)
 
         self.torch_profiler.stop()
+
+        if self.record_shapes and self.shape_discovery:
+            kernel_shape_profiler.disable()
+
         if not _is_npu:
             # Build filename with only non-zero ranks to maintain backward compatibility
             filename_parts = [self.profile_id, f"TP-{self.ps.tp_rank}"]
